public class CWH_09_operators_precedance {
    public static void main(String[] args) {
        float a = (10.0f*6.0f) / (6.0f*7.0f);
        // for getting the value according to bodmas we have to use 
        // paranthesis which we called bracket(). 
        System.out.println(a); 
        // q1-  (x-y)/2?
        // q2-  ((b*b)-(4*a*c))/(2*a);
        // q3-  (v*v)-(u*u);
        // q4-  (a*b)-d;
        
    }
}
